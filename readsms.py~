#!/usr/bin/python
import serial
import time
import subprocess
import os

DEVNULL = open(os.devnull, 'wb')

ser = serial.Serial('/dev/gsmmodem', 460800, timeout=1)
print ser.name

while True:
	line = ser.readline()
	if line.startswith("+CMTI"):
		print "new message"
	

ser.close()
DEVNULL.close()
